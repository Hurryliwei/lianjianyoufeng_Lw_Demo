<button  bindtap="getUserProfile" 
      plain='{{true}}'
     class="image-container"
     disabled="{{canClick}}" style="width:120rpx;height:120rpx;overflow: hidden;border-radius: 50%;">
  <slot name="img"></slot>
</button>